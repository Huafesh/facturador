# facturador

Este repositorio contiene el código del proyecto **facturador**, clasificado como **Aplicación Web (PHP)**.

## Tecnologías Utilizadas

- PHP
- HTML / CSS

### Ejecución

1. Aloja los archivos en un servidor local compatible con PHP (como XAMPP, Laragon, o WampServer).
2. Abre la URL correspondiente en tu navegador (ej. `http://localhost/` + nombre de la carpeta).

---
*Generado automáticamente para la organización y catalogación de proyectos.*
